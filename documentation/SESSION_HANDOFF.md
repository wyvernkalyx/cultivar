# SESSION_HANDOFF

Status: WRITE-LAST. Committed as this session's final act, 2026-08-18.
HEAD at write time is a0049910 (D137 completion refactor); this commit
rides on top.

## Preamble -- argue against yourself first

Four refutations from this session. First: the architect opened with
carried memory one session stale (it ended at 2026-08-12 and knew
nothing of the 08-13 session that shipped D146 and rank 2) AND its
project-knowledge doc copies two sessions stale (dated 08-11); the
public-repo clone settled both before any claim shipped. Second: the
architect's believed 149 fontSize literals was 148 -- its own histogram
summed to 148 and was never summed. Third: the first ratified amendment
block contained a false clause (triggerLabel filed among the 12
COINCIDENTs; the audit says PARTIAL), killed by implementer flag before
commit. Fourth: the architect's Tier 1 placement instruction
contradicted its own ratified hash; the implementer's STOP and
reconstruction proved the divergence to the byte. Trust this file only
through its Phase A predictions.

## Entry point

Rank 4: accessibility labels, session-ladder first (the prior
backlog's ordering). Its Phase A reads session-ladder.tsx's current
accessibility state at HEAD and the D107 ruling trail before any label
is authored -- labels are authored text and must not quote gated
strings (CLAUDE.md rule; D107 is the precedent). The adjacent
44pt-floor audit stays a separate banked slice; do not merge them.

## Start here (Phase A, read-only)

- origin/main at write time = a0049910cf201f4d19211f6e0d4e481af3bf6c06
  (observed push line df127bc..a004991, rev-list 0 0). This handoff
  commit rides on top, pushed as the final act. At next open expect
  origin/main = the handoff commit (subject predicted: "docs: session
  handoff -- D137 typeset completed; role layer shipped"), sync 0 0.
  If HEAD is neither, work continued past this handoff -- reconcile
  before proceeding.
- Worktree: clean except the standing two untracked
  (.claude/settings.local.json, .claude/skills/).
- Toolchain, measured at this session's build gate: 161 tests /
  3 suites (npm test, never bare npx jest); tsc 0 errors; lint exactly
  1 error (template use-color-scheme.web.ts). Migrations: 19
  well-formed names (predicted unchanged -- no schema this session;
  re-measure).
- DB: NOT read this session (no MCP call). The device smoke gate may
  have written session rows if rungs were tapped. The last pinned
  numbers are two handoffs old. Re-measure before any claim.
- If any of these don't match, the repo wins -- re-baseline before
  proceeding.

## What shipped (newest first; shas from pasted output)

a004991 refactor: D137 completion -- 57 sites collapse onto the Type role layer
6fc8017 docs: D137 amendment -- typeset completion collapses 57 sites onto a role layer

## The arcs

D137 completion (rank 3, retired). A read-only discovery enumerated
all 148 fontSize sites and classified the 80 at role sizes (41 FULL /
27 PARTIAL / 12 COINCIDENT) with complete prop sets. The finding that
shaped the ruling: the restyle slices adopted the reference's VALUES,
not tokens, and body 11.5 split on real surfaces into deliberate
weight tiers. Operator ruled descriptive tokenization: the token set
records what shipped, not the reference's five-role line -- seven
roles (display, title, label, body, value, action, serif), type props
only, tabular-nums kept per-site because the reference scopes it to
lab values rather than a tier. 57 sites collapsed with zero intended
visual change (two ratified no-op prop additions); 21 kept literal by
design; 2 consumerless FULL keys untouched; reconciliation 57+21+2=80
recorded in the amendment. The doc amendment landed BEFORE the feat
(mechanism supersession in design-overhaul.md, lines 82-127 at
6fc8017). Six zero-consumer family aliases removed (32 -> 26).

## Refuted hypotheses / memory corrections

- Carried memory and project knowledge both stale at open (Preamble);
  the clone of origin settled it.
- "149 fontSize literals" -- 148 (implementer, four independent forms).
- The first amendment block's tab-bar-label clause was false and
  double-named three of the 12 COINCIDENTs; corrected and re-ratified
  before any commit.
- Two build-prompt criteria were wrong against the state they gated:
  4a ignored theme.ts's own 7 new fontSize declarations inside the
  criterion's glob; 4c's backslash-b-anchored alternation matched the
  one genuine 10.5 site. Corrected forms (theme-scoped, comma-anchored)
  validated in the build report and reused in the commit prompt's
  staged checks.

## Operator rulings this session

Collapse policy = descriptive tokenization (grounds: the weight splits
are deliberate shipped design; the token system records reality;
normative restyling was rejected as visual churn with no lived
demand). terpeneRange (500 at 11.5) stays literal by default held.
Amendment bytes ratified twice -- re-ratified after the false-clause
fix. Device gate: passed, aggregate verdict, accepted per the standing
operator-acceptance clause.

## Refutation ledger, this session

Architect errors, enumerated (count = list length): a malformed grep
whose pattern landed as a filename (self-caught via stderr); the
hand-stated 149 (implementer-caught); a sed that corrupted the first
amendment build (self-caught by assert; regenerated clean); the Tier 1
placement instruction contradicting the architect's own ratified hash
(implementer STOP with reconstruction proof); the false triggerLabel
clause in ratified prose (implementer-flagged); build criterion 4a
(implementer-caught); build criterion 4c (implementer-caught). Seven.
Implementer: 0 errors, 5 catch credits, 1 correct unconditional STOP.
Operator: 0. Zero errors reached origin; the two that reached ratified
text were killed before any commit.

## Promotion candidates (carried + new)

1. Carried from 2026-08-13, still unpromoted: structural claims about
   third-party behavior enter a spec only after the installed source
   is read.
2. NEW: a criterion gating post-change state is executed against a
   constructed post-change tree before the prompt ships. The
   architect-side clone makes this free; errors 4a and 4c were both
   executable in advance and were not executed. Fold into the
   unexecuted-criteria rule at the next promotion pass.
3. NEW constraint: no grep gate may anchor on a bare site-list line --
   a004991's commit body now contains all 57 (implementer-flagged;
   the authored-text rule, now true of a commit message).

## Banked follow-ups (ranked backlog, then unranked)

Ranked, rank 3 retired this session: 4 accessibility labels
(session-ladder first) -- the entry point; 5 splash dark sub-key;
6 dead chrome chore (now with citation: the three consumerless
coa-detail keys badge/meta/sourceLab, 2026-08-18 audit); 7 Reduce
Motion Scope B (ruled); 8 expandable History (delta doc + D-number
first).

New banked this session:
- theme.ts Dash docblock still claims consumers reference the loaded
  families by name -- superseded by slice 2's alias collapse; the next
  theme.ts touch amends it (implementer-flagged).
- The one off-role 10.5 site (insights.tsx buyMeta territory, line
  ~580 at a004991) is data, not a defect; no action.

Carried unchanged from 2026-08-13: 44pt-floor audit + delta doc slice;
inline-surface raw-error class (four specimens); dead result.message
payloads chore; ragged wrap in delta-fab-touch.md; BottomTabInset last
consumer; bloom comment; session-ladder D83 scope note; same-name
disambiguation; Stash system-font states; last-card breathing room;
orphaned PDFs before public release; app rename collision check;
migration 20260715185455's superseded D53 comment. Plus: the
project-knowledge doc copies are now three sessions stale -- operator
refresh when convenient.

## Working rhythm

Stable method lives in CLAUDE.md. Two deltas proven this session: the
architect-side clone of the public repo is a validated channel --
end-to-end blob reads, ratified-bytes construction and hashing, %B
pins, and (unexploited until 4a/4c) pre-executing post-change
criteria; treat it as standard when the architect holds a container.
And the Tier 1 staged-blob hash gate caught a real placement
divergence on its first docs use -- exactly the class the
worktree-hash rule predicted it would catch.
